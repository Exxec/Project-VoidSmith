# Observed bugs and compatibility gaps

| ID | Status | Evidence | Impact | Next action |
|---|---|---|---|---|
| SVG-001 | Resolved | Live inspection located `mods/enabled_mods.json`. | Scan now distinguishes enabled from merely installed mods and lists missing enabled IDs. | Keep a fixture covering this configuration path. |
| SVG-002 | Resolved for the supplied scan | HJSON comment, comma, bare-key, bare-value, single-quoted-string, numeric-suffix, and semicolon-delimiter forms are normalized. | The supplied enabled/core scan parses 431 factions with no parser errors. | Keep regression fixtures. |
| SVG-003 | Resolved for the supplied scan | The refreshed current-module scan has zero parser errors; the review queue contains only three unavailable enabled IDs, not malformed `mod_info.json` files. | Nonstandard source files are no longer incorrectly reported when the actual condition is unavailable enabled-mod configuration. | Keep the queue derived from fresh scans; do not infer missing mods' content or utility status. |
| SVG-004 | Open | Current-code scan identifies `THI`, `QualityCaptains`, and `Red_stripe` as enabled but unavailable. Read-only local inspection found no corresponding directories; researched evidence identifies all three as gameplay/content mods. See `MOD_REVIEW.md`. | Their data cannot be scanned, and they must not be treated as utilities or assumed available dependencies. | Restore compatible mods or remove stale launcher IDs through normal game management, then re-scan. |
| SVG-005 | Resolved | Standard `.wpn` files carry quoted `id`, `type`, and `size` fields. | The scanner now extracts those fields lexically without interpreting engine constants. | Keep fixtures with non-JSON constants. Universal/Hybrid/Composite/Synergy mount semantics are resolved -- see SVG-013. |
| SVG-006 | Resolved | A live stale-export audit crashed because enabled mods commonly use object-form dependency declarations. | Registry creation was blocked by an unhashable dependency object. | Scanner now extracts only an explicit `id` string from string/object entries, skips unauditable entries, and does not interpret version constraints. |
| SVG-007 | Resolved | A live Advanced request with an unavailable locked weapon generated an empty but legal loadout. | Vacuous range checks incorrectly gave it an 80-point quality score, and the request serialization was not JSON-native. | Empty legal loadouts now score 0 with an explicit explanation; Advanced report fields use arrays/objects and state the effective profile. |
| SVG-008 | Resolved | The first bounded-search refactor lost the internal hull/profile lookup; targeted generation tests raised `NameError: hull is not defined`. | Candidate generation failed before validation. | Restored the lookup and added bounded-alternative regression coverage; full suite has 51 passing tests. |
| SVG-009 | Resolved | Verified against a live installation (`C:\Program Files (x86)\Fractal Softworks\Starsector\starsector-core\data\config\settings.json`): `fluxPerCapacitor`/`dissipationPerVent` confirm the per-unit *effect* but not OP cost or a hull-size maximum, both engine-hardcoded. Cross-checked against https://starsector.wiki.gg/wiki/Flux, whose stated per-unit effects (200 capacity, 10 dissipation) match the installed settings.json exactly, and which states "Adding a vent or capacitor costs 1 Ordnance Point" plus a documented per-hull-size maximum (Frigate 10, Destroyer 20, Cruiser 30, Capital 50). | `adapters/vanilla/FLUX_UNIT_COST` now backs a real vent/capacitor OP-budget and hull-size-maximum legality check for `core`-sourced hulls; `VENT_CAP_OP_UNKNOWN` remains correct fail-closed behavior for any hull whose source mod has no documented table. | Extend `adapters/` with a per-mod `FLUX_UNIT_COST` if any installed mod is confirmed (via its own settings.json or documentation) to override vanilla's values. |
| SVG-010 | Resolved differently than planned | `adapters/vanilla/INCOMPATIBLE_HULLMOD_PAIRS` stays empty by design: two independent web sources (starsector.wiki.gg's Hullmod page, and a targeted follow-up query against it) found no documented *pairwise* hullmod exclusivity mechanic in vanilla at all. What they document instead is a *categorical* limit -- `settings.json`'s `maxLogisticsHullmods:2`, matching the wiki's "limited to a maximum of 2 logistics hullmods." | `MAX_LOGISTICS_HULLMODS` and the `LOGISTICS_HULLMOD_LIMIT_EXCEEDED` legality check now implement the real mechanic. Verified against all 441 real core variants: zero false positives. The pairwise table stays empty and may not correspond to anything real in vanilla; see `docs/ROADMAP.md` Tier 1.2. | If a genuine pairwise vanilla exclusion is ever found (not a mod's own compatibility-patch feature), cite it there. |
| SVG-011 | Open | Verified against real core weapon data: only 31/201 (~15%) of core `.wpn` entries have a populated `energy/second` CSV column; 128/201 have `energy/shot` but not `energy/second` (e.g. `lightmg`: `energy/shot=3`, `energy/second` blank). Deriving a per-second rate from `energy/shot` would require modeling burst-fire timing (`burst size`, `burst delay`, chargeup/chargedown), which risks producing a confidently wrong number rather than an honest unknown. | `scoring/candidate_score.py`'s `flux_sustainability` component -- and now `generation/vent_cap.py`'s allocator -- correctly decline to guess (`flux_per_second is None` fails closed) but as a result activate on a minority of real vanilla weapons in practice, not most of them. | Before attempting a derived-from-`energy/shot` estimate, get the exact vanilla fire-rate formula from a citable source (wiki or decompiled engine behavior), not an assumption. Until then, this is a coverage limitation, not a correctness bug. |
| SVG-012 | Resolved | Live stress test (all 158 real core combat hulls x 3 profiles = 474 candidates) surfaced `BUILT_IN_WEAPON_OVERRIDDEN` on 53 generated candidates. Root cause: `generation/candidate.py::_build_candidate`'s mount loop only ever excluded `UNIVERSAL`-type mounts, not `BUILT_IN`-type ones; some real vanilla weapons declare `mountType` `"BUILT_IN"` themselves, making them spuriously pass the eligibility filter for *any* hull's `BUILT_IN` mount, independent of which specific weapon that particular hull actually hardwires there. | Tier 1.4's legality check caught every occurrence correctly (no illegal export was ever possible) but the generator was handing back candidates it should never have proposed in the first place. | Fixed: a mount present in `hull.built_in_weapons` is now always left unassigned (the game auto-fills it), same as an explicitly-empty mount. Regression test added (`test_generation_never_assigns_a_weapon_to_a_hull_fixed_built_in_mount`); re-ran the same 474-candidate stress test after the fix: 471/474 (99.4%) LEGAL, zero `BUILT_IN_WEAPON_OVERRIDDEN` occurrences, remaining 3 trace to the pre-existing Universal-mount-semantics gap. |
| SVG-013 | Resolved | The single largest remaining false-positive source across every stress test this project has run: `MOUNT_TYPE_MISMATCH` on 299/441 real core variants at session start. Root cause: both `validation/legality.py` and `generation/candidate.py` only ever did an exact string match between a mount's `type` and a weapon's `mount_type`, which is correct for `BALLISTIC`/`ENERGY`/`MISSILE` mounts but wrong for `HYBRID`/`COMPOSITE`/`SYNERGY`/`UNIVERSAL` mounts, which accept documented *combinations* of basic types. Verified via three independent, mutually corroborating sources: https://starsector.wiki.gg/wiki/Weapon and https://starsector.wiki.gg/wiki/Modding:Weapon_Slots (both state the same combinations), and -- strongest evidence -- an empirical scan of every weapon-to-mount assignment across all 431 real, developer-authored core variants in a live installation (e.g. 59 `BALLISTIC`-type and 56 `ENERGY`-type weapons installed in `HYBRID`-type mounts, matching the wiki exactly, 0 exceptions to the derived matrix). | `core/mount_compatibility.py::MOUNT_TYPE_COMPATIBILITY` now backs both `validation/legality.py` and `generation/candidate.py`. Re-verified against all 441 real core variants: `MOUNT_TYPE_MISMATCH` dropped from 299 to 4 (98.7% reduction); `LEGAL` count rose from 162 to 293 (+81%). The remaining 4 were individually inspected: all are `hull_size: FIGHTER` wings (`drone_pd_midline`, `hoplon`, `wasp`), a genuinely separate, undocumented internal fighter-mount-semantics area -- correctly left alone rather than folded into the ship-mount matrix on weak evidence. | If fighter-internal mount semantics are ever independently documented, extend the matrix (or a separate one) for `hull_size: FIGHTER` specifically -- do not assume it matches the ship-mount rules. |
| SVG-014 | Resolved | Live-verified `faction-capability hegemony`: 9 of 28 real `known_hulls` entries (`onslaught_xiv`, `dominator_xiv`, `eagle_xiv`, `falcon_xiv`, `enforcer_xiv`, `wolf_hegemony`, `kite_hegemony`, `hound_hegemony`, `buffalo_hegemony`) failed to resolve against the registry. Traced to a real, unparsed data source: `data/hulls/skins/*.skin` files (confirmed present in the live install, e.g. `starsector-core/data/hulls/skins/onslaught_xiv.skin`), which declare a `skinHullId` derived from a `baseHullId` plus overrides (`removeWeaponSlots`/`removeEngineSlots`/`removeBuiltInMods`/`removeBuiltInWeapons`/`builtInMods`/`builtInWeapons`/stat overrides) -- a real, documented, parseable JSON format the scanner did not ingest into `Hull` entities at all. | Every faction-known-hull and doctrine feature undercounted real ship variety for any faction that fields skinned hulls (XIV ships and equivalents in other mods). | Fixed: `parsers/entities.py::hull_from_skin` materializes a derived `Hull` per skin, applying only fields with unambiguous, directly-declared semantics (explicit remove/add id lists for mounts/hullmods/built-in-weapons/hints, or a single scalar override for `ordnancePoints`/`hullName`/`fighterBays`/`suppliesPerMonth`); every other real skin field this project doesn't model as a typed `Hull` attribute (`maxSpeed`, `shieldEfficiency`, `systemId`, `engineSlotChanges`, `restoreToBaseHull`, ...) is preserved verbatim in `raw`, never guessed at. `core/scanner.py` discovers `data/hulls/skins/*.skin` per mod and resolves every skin's `baseHullId` in one deferred pass after all mods finish scanning (a skin can reference a hull from a different mod than its own), never guessing at an unresolved or ambiguous base id -- surfaced as a warning instead. Stress-tested against the full live install (148 mods): hull count rose from 4,430 to 4,736 (+306 real skinned hulls), 0 exceptions, only 2 real data-quality warnings (one skin missing `baseHullId` entirely, one referencing an ambiguous cross-mod hull id) -- both correctly reported, not guessed. Re-verified `faction-capability hegemony`: `unresolved_known_hull_ids` dropped from 9 to 0. Discovered and fixed a related real gap during this same pass: 20 of the live install's own core `.skin` files use bare-leading-decimal numeric literals (`"baseValueMult":.7`), which is invalid JSON; added to the existing HJSON-tolerance normalization in `parsers/common.py::_relaxed_json`, dropping skin parse errors from 29 to 5. The remaining 5 are genuinely malformed (mismatched braces) in their own source files, not a syntax convention -- correctly left as errors rather than guessed at. | If a genuinely different mod skin format surfaces (e.g. one using fields this pass didn't see across the 66 real core `.skin` files it was verified against), extend `hull_from_skin` from real evidence, not assumption. |

| SVG-015 | Resolved | User-reported: a mod (`JaydeePiracy`) that adds a ship (`jdp_arcubus`) to an existing faction (Hegemony) was not recognized as belonging to that faction. Traced to two compounding bugs, both confirmed against the real 148-mod install. (1) `core/registry.py::EntityIndex.build` only correctly handled exactly two same-id entities: a third-or-later claimant matched neither branch (`id in by_id` was already false, popped by the second claimant; `id not in duplicates` was already false too) and was silently dropped -- not indexed, not counted as ambiguous, gone. Real installed data has 24 separate mods each shipping their own same-id `hegemony.faction` file (a common, real Starsector modding convention: a small partial file with only `knownShips`/`hullFrequency`/`priorityShips`, no `id`/`displayName`, that the game engine patches into the base faction); only the first two of those 24 ever reached `duplicates`, the other 22 vanished outright. (2) Even the two that did reach `duplicates` were never merged: every faction-aware caller (`api.py::resolve_faction`, `Registry.faction_equipment`, `run_generate`'s STRICT_FACTION/FACTION_PLUS resolution) either raised "ambiguous" or silently returned `None`/one arbitrary source's incomplete list the moment more than one mod touched the same faction id -- `run_generate`'s `registry.factions.by_id.get(faction_id)` in particular degraded to *no faction data at all* with no error, for any of these 24-way-collided factions. | Faction-aware generation, doctrine, capability, gap-recommendation, and Why-Not output silently lost real, correctly-declared modded content for any faction with more than one contributing `.faction` source -- confirmed to include not just Hegemony but every core faction most real modlists touch this way. | Fixed at both layers: `EntityIndex.build` rewritten to a three-way `if/elif/else` that appends every claimant past the second into `duplicates` rather than dropping it. `Registry.resolve_faction(faction_id, source_mod=None)` is a new canonical resolution path that, absent an explicit `source_mod` override, unions `known_hulls`/`known_weapons`/`known_fighters`/`known_hullmods` across every same-id source (each entry is real, independently declared evidence, so unioning never fabricates a claim) and takes identity fields (name/tags) from whichever source has the most complete identity evidence; `Registry.faction_contributing_sources(faction_id)` exposes the real source list for reporting. `api.py::resolve_faction`, `Registry.faction_equipment`, and `run_generate`'s faction resolution now all route through it. Live-verified: `svg query faction-equipment --faction-id hegemony` against the real install now returns 108 known hulls (up from 37 core-only) including `jdp_arcubus`, sourced from all 24 real contributing mods. 14 new regression tests (`tests/test_entity_index.py`, `tests/test_faction_resolution.py`); full suite 512 passing, 0 regressions. | If a mod is ever found shipping a same-id `.faction` file that has real, conflicting identity fields (not just an additive `knownShips`-only patch), reassess whether merging identity is still correct for that case, or whether it should stay a hard ambiguity requiring `--source-mod`. |

| SVG-016 | Resolved | User-reported ("the slots don't line up with the actual ship") against a real screenshot of the shipped GUI's ship-fitting canvas. Traced to `gui/main_window.py::TechnicalCanvas.show_hull` computing weapon-mount scene positions as `location - center` -- but per the Starsector modding wiki (`.ship File Overview` / `Modding:Weapon Slots`, https://starsector.wiki.gg/wiki/File_overview:_ship): "the defined center of the ship is defined as a set of Cartesian coordinates relative to the most bottom-left pixel of the sprite. The defined center point is the relative reference point for ... weaponSlots" -- i.e. `locations` are *already* relative to `center`, so subtracting it a second time silently shifted every mount box away from its real position (by up to half the hull's declared width/height). The sprite's own placement offset had an equivalent bug: it used the sprite's raw pixel top-left as the anchor instead of accounting for `center` being measured Y-up from the sprite's bottom-left. | Every mount marker on the ship canvas (added earlier this session) was positioned wrong relative to the sprite -- clicking "on" a visible turret would usually miss it entirely, undermining the whole point of the redesign. | Fixed both formulas per the cited convention: mount position is now `(loc_x, -loc_y)` (Y-flip only, for Cartesian-Y-up game data vs. Qt's Y-down scene), and the sprite offset is now `(-center_x, center_y - sprite_height)`. Verified against three real core hulls pulled from the live install and rendered off-screen: Wolf (a simple frigate) -- 5 of 6 mounts now land exactly on visible turret positions in the sprite art, with the 6th (a documented nose-mounted medium hardpoint) floating just forward of the hull, consistent with that hull's real, visibly-protruding nose gun rather than a remaining bug; Hammerhead and Atlas Mk.II show a similar pattern (mounts land correctly except ones on real, visibly-protruding structures like shoulder turrets or extended mining arms not baked into the base hull sprite). New regression test (`tests/test_gui_canvas.py::test_mount_box_position_is_cartesian_relative_to_declared_center_not_double_subtracted`) locks in the exact transform against a synthetic fixture with a known center/location pair. Full suite: 533 passing, 0 regressions. | If a mount is ever found floating clearly wrong on a hull with no visible reason (no protruding structure at that location in its real sprite), re-verify against the cited wiki convention rather than assuming this fix is universally exact -- some mods may author `.ship` data slightly differently. |

| SVG-017 | Resolved | Live stress test (ROADMAP Phase 26) against real Diable Avionics 2.9.5.3 data (one of five real mods the phase specifically targeted) surfaced a real parser error: `data\hulls\diableavionics_IBBgulf.ship` failed to parse entirely (`Expecting value: line 295 column 24`), traced to `"renderOrderMod":+30` -- a leading unary-plus numeric literal, invalid JSON, not covered by the existing HJSON-tolerance normalizer (`parsers/common.py::_relaxed_json`, which already tolerates comments/trailing commas/bare keys/single-quoted strings/numeric suffixes and, per SVG-014, bare-leading-decimals like `.7`). Scoped the real install (149 mods) for the same pattern: exactly 1 occurrence anywhere, on a field (`renderOrderMod`, purely a rendering z-order hint) this project does not model as a typed `Hull` attribute. | The entire real `diableavionics_IBBgulf` hull entity was silently skipped (correctly logged as a skipped entity/parser error, per this project's fail-closed philosophy, but still real content lost to a trivial, safe-to-tolerate syntax variant). | Fixed: extended `_relaxed_json` with a second structural-character-anchored regex (mirroring the existing bare-leading-decimal fix's approach) that strips a `+` immediately following `:`/`,`/`[` before a digit, leaving `+` inside string literals and any other position untouched. Verified against the real file (now parses; `hullId` `diableavionics_IBBgulf` present) and against the whole install (parser error count for `diableavionics` dropped to 0; no other mod affected, since the pattern occurred nowhere else). New regression test `tests/test_parsers.py::test_relaxed_json_accepts_leading_unary_plus_numbers` (synthetic fixture, including a literal `+7` inside a string value to prove string content is untouched). Full suite: 540 passing (539 + 1 new), 1 skipped, 0 regressions. Distinguished during the same pass from a genuinely different real parser error in Dassault-Mikoyan Engineering's `6eme_bureau.faction` (`Extra data: line 332 column 1`): directly diff-verified as a real brace-count mismatch (36 `}` vs 35 `{`) in that mod's own source, not a tolerable syntax convention -- correctly left as a reported error, consistent with SVG-014's same distinction for its remaining 5 unfixed `.skin` parse errors. | If a leading-unary-plus literal is ever found on a field this project does model (unlike `renderOrderMod`), confirm the now-fixed normalizer feeds it through correctly end-to-end (it should, since normalization happens before any field-specific parsing), but re-verify rather than assume. |

| SVG-018 | Resolved | Live cross-checking of `svg recommend`/`svg why-not` against a real 148-mod install (ROADMAP.md Phase 11) had already found and fixed one instance of this class of bug for the native leg's non-`--build-archetype` form. A synthetic-fixture reproduction (`tests/test_gap_recommendation.py::RetrofitAuditConsistencyTests`/`AcquisitionAuditConsistencyTests`) confirmed the same disagreement independently existed, unfixed, in the retrofit and acquisition legs: under a build-aware heuristic set (`baseline_0.4`+, including the CLI's real default `baseline_0.7`), `recommend_retrofit_solutions`/`recommend_acquisition_solutions` rank every real `Hull + BuildArchetype` combination the build-aware search produces, while `explain_retrofit_candidate`/`explain_acquisition_candidate` unconditionally used a legacy hull-only, native-top-N-truncated (retrofit) or non-build-aware, non-diversity-selecting (acquisition) candidate pool regardless of heuristic set -- both under-reporting "not considered" for a hull/build the real search DID examine and recommend, and over-reporting "recommended" for one it did not. | `svg why-not`'s retrofit/acquisition explanations could genuinely disagree with `svg recommend`'s real output for the same faction/role/hull under the CLI's own real default heuristic set -- exactly the class of drift Phase 11 had only partially closed. | Fixed by ROADMAP.md Phase 32: introduced `RecommendationAudit`, one reusable per-candidate audit-trail type, and routed both the ranking path (`recommend_native_solutions`/`recommend_retrofit_solutions`/`recommend_acquisition_solutions`) and the Why-Not path (`explain_native_candidate`/`explain_retrofit_candidate`/`explain_acquisition_candidate`) through one shared construction function per leg (`_native_audit_trail`/`_retrofit_audit_trail`/`_acquisition_audit_trail`), so they can no longer independently reconstruct ranking logic that drifts apart. Also found and fixed a related, previously entirely unexposed gap while adding the regression coverage: none of the three Why-Not explanation types (`WhyNotExplanation`/`RetrofitWhyNotExplanation`/`AcquisitionWhyNotExplanation`) carried a `confidence` field at all, so no test could have proven confidence identity even where score/rank identity held; added `confidence` to all three, computed via the same `_recommendation_confidence`/new `_capability_gap_confidence_inputs` helper the ranking functions use. Regression tests assert exact (`assertEqual`, not "close enough") identity of every scoring/confidence/evidence component the ranking function produced for each real candidate, across all three legs, for every hull in a structurally-varied 5-hull fixture. Full suite: 607 passing, 1 skipped, 0 regressions. |

| SVG-019 | Resolved | Found while fixing mypy type errors in `api.py`/`cli/main.py` (a `cli/main.py` `mypy` error at the `why-not` command's print block flagged an attribute access mypy couldn't verify against `api.run_why_not`'s real declared return type). Traced to a genuine logic gap, not just a missing annotation: `api.run_why_not` returns `CombinedWhyNotExplanation` (with `.native`/`.retrofit`/`.acquisition`, each carrying its own `.reason`) only for the legacy hull-level form, but returns `BuildWhyNotExplanation` (a flat `.reason`, no `.native`/`.retrofit`/`.acquisition` at all) whenever the caller passes `--build-archetype`. `cli/main.py`'s `why-not` command unconditionally printed `explanation.native.reason`/`.retrofit.reason`/`.acquisition.reason`. | Every real `voidsmith why-not <faction> <role> <hull> --build-archetype <id>` invocation crashed with `AttributeError: 'BuildWhyNotExplanation' object has no attribute 'native'` instead of printing its report -- a documented CLI flag (`why-not --build-archetype`, `GAP_RECOMMENDATION_ENGINE.md` section 13) that never worked end to end. No existing test exercised `cli/main.py`'s `why-not` command at all (only `api.run_why_not`/`explain_build_candidate` directly), so the gap went undetected. | Extracted the print logic into a new, directly testable `cli/main.py::_why_not_report_lines(explanation)` that `isinstance`-narrows on `BuildWhyNotExplanation` and prints a single `build: <reason>` line for that case, otherwise the original 3-line native/retrofit/acquisition report unchanged. New regression tests in `tests/test_cli_main.py` construct both explanation shapes directly (synthetic dataclass fixtures, no scan/registry needed) and assert the exact printed lines for each; the `CombinedWhyNotExplanation` case is byte-identical to the previous behavior. Full suite: 757 passing (755 confirmed starting baseline + 2 new), 1 skipped, 0 regressions. | If `run_why_not`'s return type is ever extended with a third shape, extend `_why_not_report_lines`'s `isinstance` branches (and its own test) rather than falling back to the old unconditional-attribute-access pattern. |

| SVG-020 | Resolved | Found while fixing mypy type errors in `analysis/mechanical_archetypes.py` (mypy flagged `registry.weapons` at two lines inside `infer_hull_feature_vector` as a `Registry | None` union-attr access with no preceding `None` check). Traced to a genuine logic gap: `infer_hull_feature_vector(hull, registry=None, variants=None)` independently defaults both parameters, and a caller may supply an explicit `variants` tuple while leaving `registry` `None` -- the function's own line computing `variants` (`variants if variants is not None else (registry.variants_for_hull(hull.id) if registry is not None else ())`) already anticipates `registry` being `None`, but the per-variant weapon-evidence loop right below it unconditionally dereferenced `registry.weapons.by_id` whenever a variant had any weapons, with no guard. No existing real caller happens to pass `variants` explicitly while omitting `registry` (confirmed by auditing every call site in `src/` and `tests/`), so this was latent, not yet triggered in practice. | Any future or external caller supplying `variants` without `registry` would crash with `AttributeError: 'NoneType' object has no attribute 'weapons'` instead of getting a feature vector with weapon-level evidence simply omitted (the same graceful-degradation the function already provides for a variant with zero weapons). | Fixed: the per-variant weapon-evidence branch now also requires `registry is not None`, so it degrades to "no missile/PD/long-range evidence for this variant" (identical to the existing no-weapons case) rather than crashing; fighter-wing evidence, which needs no registry, is unaffected. New regression test `tests/test_mechanical_archetypes.py::test_explicit_variants_without_a_registry_do_not_crash` calls both `infer_hull_feature_vector` and `infer_mechanical_archetypes` directly with `registry=None` and a non-empty `variants` tuple containing a weapon-equipped, fighter-wing-equipped variant, and asserts the missile/PD/long-range fractions are `None`, the fighter-wing fraction is still computed, and neither call raises. Full suite: 755 confirmed starting baseline + 1 new, 0 regressions. | If a real caller is ever found relying on `variants` being supplied without `registry`, confirm the silently-omitted weapon evidence is acceptable for that caller rather than assuming it always is. |

| SVG-021 | Resolved | Found while fixing mypy type errors in `gui/main_window.py` (`_populate_missing_retrofits`'s dense one-line `_run(...)` call flagged four related errors: a `Hull | None` union-attr access with no guard, a `completed` callback typed to return `None` that a lambda actually returned a 2-tuple from, and two `func-returns-value` warnings on the tuple's own elements). Traced to two compounding issues in that one call. (1) The background `operation` lambda read `self._current_hull.id` directly rather than capturing `hull_id = self._current_hull.id` as a local first, unlike every sibling background operation in this file (`_generate`, `_refit`, faction analysis, export) which already captures exactly this way specifically because `_run`'s operation only actually executes once its worker thread starts, by which point the user may have changed or cleared the hull selection. (2) The `completed` callback was `lambda result: (self.refit_detail.setPlainText(...), self._refresh_editable_retrofit_library())` -- a tuple literal used as an ad hoc "two statements in one lambda expression" trick: both calls did execute (tuple construction evaluates every element), so this specific instance was not silently broken, but it built and discarded a throwaway `(None, None)` return value, was untestable as an isolated unit (no name to call directly), and depended on a fragile "comma happens to mean sequencing here" reading with no compiler check that a future third addition wouldn't quietly do the wrong thing. | (1) is a real, if narrow-window, latent crash/wrong-target bug: rapidly deselecting or reselecting a hull between clicking "Populate Missing Retrofits" and the background thread actually starting could generate a retrofit for the wrong hull, or crash the background worker with `AttributeError: 'NoneType' object has no attribute 'id'` on a cleared selection, inconsistent with every sibling operation's own established defense against this exact race. (2) was accidentally correct today but not verifiably so, and not adaptable safely. | Fixed: `_populate_missing_retrofits` now captures `hull_id = self._current_hull.id` before constructing the lambda, matching `_generate`'s own established pattern exactly. The inline tuple-hack `completed` lambda was extracted into a real, directly-callable, properly `-> None`-typed method, `_apply_populate_missing_retrofits_result`, doing the same two statements as actual sequential statements. New regression tests in `tests/test_gui_canvas.py`: one captures `_run`'s real `operation` argument, changes `_current_hull` to simulate the exact race (no real QThread involved -- background-thread completion timing isn't reliably observable in this headless harness, see the adjacent `test_apply_incremental_scan_outcome_...` test's own docstring for why), and asserts the captured `hull_id` -- not the now-changed selection -- is what the operation still targets; the other calls `_apply_populate_missing_retrofits_result` directly with a real `RetrofitAvailability` fixture and asserts the detail text. Full suite: 755 confirmed starting baseline + 3 new (2 here + 1 in SVG-020's fix), 0 regressions; `tests/test_gui_canvas.py` specifically: 77 passing (75 + 2 new). | If another `_run(...)` call site is ever found reading a `self._current_*` attribute directly inside its background `operation` lambda instead of capturing a local first, apply the same local-capture fix -- this is a repeatable class of bug in this file's async pattern, not a one-off. |

| SVG-022 | Resolved | Found while fixing the last remaining mypy errors in `analysis/fleet_support.py` (`analyze_player_fleet`'s structurally-ineligible branch flagged `Argument 1 to "append" of "list" has incompatible type "str | None"; expected "str"`). Traced to a genuine logic gap: `FleetSelection` has two mutually exclusive identifiers (`hull_id` and `variant_id`), and the same loop already computes a `label = selection.variant_id or selection.hull_id or "<empty>"` a few lines above specifically to have one working identifier regardless of which form the player used -- but the structurally-ineligible branch appended the raw `selection.hull_id` instead of `label`, which is always `None` for a variant-only selection. | For a variant-only player selection (`FleetSelection(variant_id=...)`, no `hull_id`) that resolves to a structurally ineligible hull (e.g. a fighter wing loadout), `PlayerFleetProfile.excluded_selection_hull_ids` silently recorded `None` in a tuple documented and typed as `tuple[str, ...]`, instead of the variant's own id -- an evidence gap masquerading as a well-typed result, surfaced to the GUI's "Structurally excluded selections" line as a blank/`None` entry rather than the actual selection. | Fixed: that branch now appends `label` (the same fallback already computed and used elsewhere in this function), matching hull_id-selection behavior exactly and giving variant-only selections their own real identifier. New regression test `tests/test_fleet_support.py::test_structurally_ineligible_variant_selection_is_recorded_by_its_own_label_not_none` builds a variant-only selection resolving to a `FIGHTER`-sized hull and asserts `excluded_selection_hull_ids == ("fighter_loadout",)`. Full suite: 763 confirmed starting baseline + 1 new, 0 regressions; `mypy src/` now reports zero errors across all 92 source files (down from the 241 found at the start of this pass). | None outstanding; this closes out the mypy-error-fixing pass covering all previously-241 errors across the whole source tree. |

| SVG-023 | Open | Found while reviewing Ruff findings (`RUF034 useless-if-else` in `analysis/build_archetypes.py::infer_build_archetypes`). `BuildArchetypeProfile.ai_suitability` is computed as `"GOOD" if maturity == "VIABLE" and style != "FLANK_AND_COMMIT" else "CONDITIONAL"` -- AI is rated down specifically for `FLANK_AND_COMMIT` (the `FINISHER` build), matching real Starsector AI's documented difficulty executing aggressive timed-flank maneuvers. The very next field, `player_suitability`, was `"GOOD" if style == "FLANK_AND_COMMIT" else "GOOD"` -- both ternary branches return the identical literal, so the condition is dead: every build, regardless of style or maturity (including `EXPERIMENTAL`-maturity builds), is unconditionally reported as `"GOOD"` for a player to fly. | `player_suitability` currently carries zero information -- it cannot distinguish a mature, well-fit build from a barely-viable `EXPERIMENTAL` one, or a straightforward `HOLD_LINE` build from a `FLANK_AND_COMMIT` one that demands more player skill/attention, contrary to what the field name and its `ai_suitability` sibling both imply it should do. No test in `tests/test_build_archetypes.py` currently asserts a `player_suitability` value (a gap in its own right), so nothing currently depends on the always-`"GOOD"` behavior. The shape of a plausible fix (e.g. mirroring `ai_suitability`'s maturity gate without the `FLANK_AND_COMMIT` exclusion, since a player -- unlike the AI -- can execute a flank-and-commit build, so `"GOOD" if maturity == "VIABLE" else "CONDITIONAL"`) is a domain/design call this project's own established caution around AI/player-suitability framing (`ROADMAP.md`'s `STATIC_CONTROL_SUITABILITY`, explicitly never "AI combat prediction") makes worth a deliberate human decision rather than an inferred fix. | Not fixed. The dead ternary was collapsed to the literal `"GOOD"` it already always evaluated to (closes the Ruff finding, zero behavior change) with an inline comment pointing here. The real semantic gap -- what `player_suitability` should actually encode -- is left for a deliberate decision. | Decide the intended `player_suitability` semantics (likely mirroring `ai_suitability`'s maturity gate, omitting the `FLANK_AND_COMMIT` exclusion), implement it, and add the currently-missing regression test coverage for both suitability fields in `tests/test_build_archetypes.py`. |

Do not edit game or mod source files to resolve these items. A source-specific adapter must preserve raw data and carry tests before it is enabled.
